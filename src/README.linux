Building on Linux
=================

This was tested using Ubuntu 6.10, using gcc 4.1.2

To build in ../../build/linux:
$ make -f Makefile.linux

## Requirements:

sudo apt-get install build-essential flex bison subversion automake libtool libpng12-dev libjpeg62-dev libexpat1-dev xorg-dev libflac++-dev libvorbis-dev libvorbisidec-dev libasound-dev libreadline6-dev